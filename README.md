# Life Tracker

Daily tracker: vitamins, nofap, coding hours, energy.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt